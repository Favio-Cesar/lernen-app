import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lernen.mobile',
  appName: 'Lernen IO',
  webDir: '../web/.output/public'
};

export default config;
